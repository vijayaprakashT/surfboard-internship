# SEPT 26 

## INTERNSHIP :
      1.Today we dont have a internship class  so i started to continue with my task given my mentor .

      2. still I'm  working in database , today i'm going to  write a code for update function .

      3.In update function , what we have to do is employee can  update employee details whenver he has any changes in the employee detail .

      4. for this i have to write update query for updation.

      5. for updation i have created a function for that and then inside the function i have written a update query . and one more thing in entire database  we have used await ,try and catch for each function. these are the method playing major role in handling the errors and used to indicate

      6.we have used asynchronous is nothing but its used to execute number of functions at a time .It wont wait for single function to execute completely.

      7. while calling the query we have used await function is nothing but is used for indicate wait until it completes and goes for next function 

      8.try and catch mainly  used for handling the errors if it affect the database application will get crash 

      9.here what happens if that any error found while executing the block of code in  try and  catch  will handle those errors .

      