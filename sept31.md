# SEPT 31

## INTERNSHIP CLASS:
       1.In today class we have discussed some simple code to show how execution stack works .

       2. we have discussed about the scope of variable  like it will works like for example if we mention some varibale inside some function but we didnt assign a value in this situation it will check the variable name has assigned any value in code it will check top to bottom if it has it will take that value otherwise it will throw an error . related to this we have discussed some code.

    
### TASK :
        1.Do some changes in delete operation . To implement soft deletion method for have add column in existing table in database . for that  i have written select statement to get the employee detail based on request employee id.after the we will find the employee id which matches anyone of the employee id in the table if it matches otherwise it will throw an error .

        2.I have used alter table in employee detail to add a column names as is_deleted column to update whether the person is deleted or not .

        3.if its deleted we written query for update statement to update the column if a person is deleted the details  and shown as response deleted and in database it will update in the is_deleted column in the table.

        4.after this my mentor told some changes in get all function to get information based on the order like if i need details in ascending order it should  response same it goes for ascending order. and changes in create function like validating each string it should throw an error before getting to database for this i have started to googling like how to do this and implement that .
        

    
