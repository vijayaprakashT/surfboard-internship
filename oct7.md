## OCT 7:

## TASK :
    
    1. .THEN :Its a method that takes a callback function as an argument. This callback is executed when the promise is fulfilled.

    2. .CATCH  : Its a method that takes a callback function as an argument. This callback is executed when the promise is rejected(fails).

    3.Again back to the task still i'm doing the attendance task still in the process entry (attendance ) operation beacuse i got error in  while showing as response so finding those errors maybe query syntax is wrong .

    4.after changes it showing response that what i expected then  we have to join the tables to give the result in get and getall function .