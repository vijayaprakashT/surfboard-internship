# OCT 17:
   
    1. today we dont have internship class.

## TASK :
      1. now I'm doing getall function and have to do some changes .

      2 . i have used ternary operator to perform ascending or descending . ternary operator is almost like if else condition . 
     
      3.syntax for ternary operator: condition ? exprIfTrue : exprIfFalse.

      4.i have to pagination for example in request if i entered page 1 it should as response based on the fetch and offset method .

      5.pagination :
             1. Pagination is the process of dividing a large set of data into smaller.

      6.fetch and offset:
              1.FETCH :
                    1. The FETCH argument is used to return a set of number of rows. FETCH can’t be used itself, it is used in conjunction with OFFSET.

                    2.conjucntion means used to combine multiple conditions in a where clause using loogical operators such as AND,OR and NOT.
                
              2.OFFSET: 
                   1.The offset argument is used to identify the starting point to return rows from a result set. Basically, it exclude the first set of records.

                   2.offset can only be used with ORDER BY clause. It cannot be used on its own.
                   
                   3.offset value must be greater than or equal to zero. It cannot be negative, else return error.