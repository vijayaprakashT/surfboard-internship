# SEPT 24 

## INTERNSHIP CLASS:
         1. In internship class we have discussed about the database and how it works 


## DATABASE :
         1. database used store the large amount of data.

         2.It stored the collection of information.

         3.It can't be isolation because when its integrate with  for example take numbers when its perform addition its information like that you can take tables( ex: 2 tables)

         4. everything is a information until it becomes isolate (3rd point).
         
         5. data is a raw fact which describes a attributes of an entity.

         6.currently we are using relational database .these database will store the data in the form of tables .In early 70's they used to stored in the form of files .
         
### VALIDATION :

            1. we have discussed about the token analysis ,syntax and semantics . these are the main for making successfull compilation process.

            2.In programming language token analysis will check the combination of expressions  and syntax will check the rule . computer doesnt know anything until we say what to do. It will check whether the tokens,syntax and semantics when it passes the three stages it will do the compilation process and convert the code into machine readable 0,1 

            3.when it fails three stages it wont perform a compilation process.

### TASK :
        1. currently I'm doing employee details  in  crud to update the details in database 

        2. i have created employee details which includes first name , middle name, last name ,role, dob , experience , email id , phone number,address.

        3. for this  i have a created a table in pstgresql. after post method i started to doing get method .

        4. get method outline. It works like it i enter a employee id in payload it will check whether employee id is valid or not .if ist valid it give the details of employee details otherwise it will shown response as data not found.