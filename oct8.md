# OCT 8:

## INTERNSHIP CLASS : 
        1.In internship class , for loop function is discussed how loop works .

        2.for loop syntax:
             1.for (initialization; condition; increment/decrement) {
                  // code to be executed
                   }
        
        3.In intialization  we have intialize the variable  and have to apply condition in the condition part after  the if the condition pass it will for execution process and it will increment the value if not it will not get into the execution of process.

        4.example code FOR LOOP:
             ` void main 
             {
                int a = 0;
                void incrementgivenvalue(){
                    for(int i = 0 ; i < 10 ; i++){
                        a++;
                    }
                }
                incrementgivenvalue(a)
                print(a)
             }`

### TASK : 
        1. again i have to do some changes like changed everything into camelcase letters.

        2. now the task is while getting employee detail it should come with the attendance record in response.

        3. for this we have to write a query for getting single employee detail at a time .

        4.already we wrote logic for no one can access employee detail except admin .

        5. now we have show response with attendance record while showing records it has to show previous records with the present record for that there a keyword in sql which is limit.

        6.LIMIT : The limit clause in SQL allows users to control the amount of data retrieved and displayed in the result set.
         